import { BigHeading } from "./GeneralComponents/BigHeading";

export const NotFound = () => {
    return (
        <BigHeading name="Sorry, we couldn't find the page you're looking for." />
    );
};
