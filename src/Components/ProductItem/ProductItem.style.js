import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((t) => ({
    subHeading: {
        paddingTop: "30px",
        paddingLeft: "0px",
    },
    paddingAround: {
        padding: "20px",
    },
}));
